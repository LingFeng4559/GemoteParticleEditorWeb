# Gemote Particle Editor Web 完整優化計畫書

更新日期：2026-08-02  
主要專案：`GemoteParticleEditorWeb-main`  
參考專案：`Particle-Converter-main`  
素材來源：`ciaccodavi_acg_images`（目前共 100 張 PNG）

## 1. 專案目標

將現有 Gemote 粒子編輯器升級為可處理圖片素材、巢狀圖層、複雜 3D 動畫、可控制匯出負載，並能從帶有編輯器註解的 YML 恢復專案結構的非破壞式編輯工具。

核心使用情境：

1. 從單張圖片、多張圖片或資料夾載入任意 PNG/JPG，轉換為可編輯粒子圖層。
2. 使用群組、子圖層、顯示、隱藏、Solo、鎖定和匯出開關管理複雜作品。
3. 為每個圖層設定位置、旋轉、縮放、Pivot 和時間。
4. 支援 A 圖左旋 360°、B 圖右旋 360°，在相同空間與時間內交疊播放。
5. 將動畫安全地烘焙成 Gemote YML，並控制指令數量與伺服器負載。
6. 透過 `#@gemote-*` 註解從 YML 恢復圖層、群組及動畫資料。

## 2. 目前狀態稽核

### 2.1 已完成

- 專案格式 3.0、舊資料遷移和圖層欄位正規化。
- 巢狀圖層與群組資料模型。
- 圖層顯示、匯出、鎖定、Solo、展開與刪除。
- 圖層拖曳排序，以及將圖層拖入群組。
- 單張、多張和資料夾圖片匯入。
- 圖片透明門檻、縮放、取樣間隔、粒子間距、平面與顏色模式。
- 靜態粒子使用 `THREE.Points` 點雲渲染。
- 非破壞式位置、旋轉、縮放與 Pivot 資料模型。
- Spin 修改器及正負 360° 求值。
- 父群組 Transform 疊加到子圖層。
- 基礎時間軸播放、停止、拖曳及總長度設定。
- 動畫烘焙及 12,000 指令上限。
- YML 圖層註解、粒子歸屬註解與反解析。
- `.gemote3d`、`.json`、`.yml`、`.yaml` 專案載入入口。
- 目前 8 項自動測試全部通過。

### 2.2 已有基礎，但尚未完整

- Transform 模型包含旋轉與縮放，但介面目前只提供位置和 Pivot；基礎旋轉、縮放仍無輸入控制。
- `tracks` 已能儲存，但沒有關鍵影格建立、刪除、移動、插值和時間軸軌道 UI。
- `timing` 包含 `startTick`、`duration`、`loop`、`loopMode`，但目前求值器尚未真正套用 repeat、ping-pong 和 once。
- Spin 支援起訖角度與緩動，但每層目前只編輯一個 Spin，缺少多修改器堆疊管理。
- YML 註解可以恢復編輯器輸出的資料，但對沒有註解的第三方 YML 只能建立未分組圖層，尚無智慧分組。
- 動畫烘焙有全域指令限制，但尚無匯出前預估報告、每層預算和使用者品質選項。
- 拖放已具備基本功能，但缺少放置位置提示、同層「之前／之後」判定與鍵盤排序。
- 測試以純模型單元測試為主，尚未涵蓋完整瀏覽器流程及大型素材壓力測試。

### 2.3 完全未完成

- 素材縮圖瀏覽器。
- IndexedDB 圖片素材庫、標籤、搜尋、收藏和最近使用。
- 從 100 張 `ciaccodavi_acg_images` 建立可重複使用的素材索引。
- Position、Rotation、Scale、Opacity/Visibility 關鍵影格軌道。
- Orbit 公轉修改器、路徑動畫及朝向路徑。
- 多選圖層、批次修改、複製圖層和動畫預設。
- 完整 Undo/Redo 命令歷史；目前 Undo 只移除最後粒子或群組。
- 時間軸縮放、標記、工作區段及播放 FPS 選擇。
- Web Worker 圖片轉換與動畫烘焙。
- 資產與粒子資料的記憶體／快取生命週期管理。
- 完整繁中、簡中、英文的新功能翻譯。
- 新功能操作文件與範例專案。

## 3. 實作原則

- 原始粒子座標永遠保留，Transform 與動畫只在預覽或匯出時求值。
- 圖層樹是專案結構的唯一真實來源，不另外維護容易不同步的 UI 樹。
- 預覽與匯出共用同一套動畫求值器，避免畫面與 YML 結果不同。
- 所有高成本操作提供預估、取消、進度和負載上限。
- 編輯器註解必須保持為合法 YML 註解，不影響 Gemote 讀取。
- 每個階段必須包含資料遷移、自動測試及瀏覽器驗收。

## 4. 後續實作階段

### Phase 1：修正現有 MVP 與建立可靠基線

工作內容：

- 補齊現有 TimelinePanel 輸入事件、播放狀態和選取切換測試。
- 修正新版 ES Module 的瀏覽器快取更新策略。
- 確認 Spin 為 `-360` 時，儲存、重載、預覽與匯出皆維持負方向。
- 增加 StateManager、SceneSync、CodeGenerator 整合測試。
- 建立測試用 A/B 雙環專案 fixture。

驗收條件：

- A/B fixture 在 tick 0、20、40、60、80 的位置符合預期。
- 儲存再載入後所有角度、Pivot、父子關係不變。
- 瀏覽器無未處理例外，Git 工作區乾淨。

### Phase 2：完整 Transform 編輯器

工作內容：

- UI 補齊 Position XYZ、Rotation XYZ、Scale XYZ、Pivot XYZ。
- 增加統一縮放鎖、重設、複製和貼上 Transform。
- 提供 Pivot：粒子中心、包圍盒中心、世界原點、場景點選。
- 增加 3D Transform Gizmo，並遵守圖層鎖定狀態。
- 選取框、包圍盒和操作手柄顯示動畫求值後的位置。

驗收條件：

- UI、Gizmo、專案檔、YML 註解四方資料一致。
- 父群組與子圖層 Transform 疊加順序有單元測試。

### Phase 3：真正的關鍵影格時間軸

工作內容：

- 定義 Track/Keyframe schema：property、tick、value、interpolation。
- 支援 Position、Rotation、Scale 和 Visibility 軌道。
- 實作 linear、step、ease-in、ease-out、ease-in-out 插值。
- 支援新增、刪除、拖曳、複製、貼上、多選關鍵影格。
- 套用 `startTick`、`duration`、`loop`、`once`、`repeat`、`ping-pong`。
- 時間軸加入刻度縮放、工作區段和逐格前後移動。

驗收條件：

- 任一圖層可同時具有位置、縮放及旋轉軌道。
- ping-pong 在邊界 tick 不重複或跳格。
- 多圈旋轉角度不可被自動正規化成 0–360°。

### Phase 4：複雜動畫修改器

工作內容：

- Spin 支援同層多個實例與修改器排序。
- 新增 Orbit：中心、軸、半徑、起訖角、圈數、方向。
- 新增 Offset/Noise/Wave 修改器。
- 支援群組修改器影響所有子層。
- 建立「雙向交疊旋轉」、「公轉＋自轉」、「多環花紋」預設。

驗收條件：

- A 左旋、B 右旋可各自設定不同 Pivot、速度和圈數。
- 同一圖層可同時公轉與自轉，預覽與烘焙座標一致。

### Phase 5：素材資產庫

工作內容：

- 建立 IndexedDB：asset id、檔名、Blob、縮圖、尺寸、標籤、時間戳。
- 首次選取 `ciaccodavi_acg_images` 時匯入 100 張 PNG 並產生縮圖。
- 提供網格／清單模式、搜尋、標籤、收藏、最近使用和刪除。
- 點擊或拖曳縮圖即可建立粒子圖層。
- 保存每個素材最近一次轉換參數。
- 顯示原圖尺寸、估計粒子數和透明像素比例。

驗收條件：

- 第二次開啟不需重新選取檔案即可顯示已存素材。
- 100 張素材可搜尋、排序並逐一匯入。
- 刪除素材不會刪除專案中已產生的粒子圖層。

### Phase 6：圖片轉換與 3D 效能

工作內容：

- 將圖片解析、取樣和動畫烘焙移到 Web Worker。
- 增加最大粒子數、預估粒子數與超限確認。
- 點雲採用分批更新、幾何快取和 dirty flag。
- 播放時只更新具有動畫或父層動畫的圖層。
- 大型場景提供預覽降採樣，匯出仍可選完整品質。
- 蒐集 FPS、求值時間、指令數和記憶體估計。

驗收條件：

- 10,000 粒子編輯操作不長時間凍結主執行緒。
- 100 張素材批次建立縮圖時有進度與取消。
- 播放停止後不殘留 requestAnimationFrame 或幾何資源。

### Phase 7：YML 匯出與反解析強化

工作內容：

- 匯出前顯示影格數、粒子指令數、估計檔案大小和取樣步距。
- 品質模式：最高品質、平衡、伺服器安全、自訂上限。
- 每層可設定烘焙步距與匯出啟用區間。
- 註解加入 checksum、schema version 和 source project id。
- 對無註解 YML 提供依空行、delay、粒子類型和相鄰性分組選項。
- 反解析錯誤提供行號、原因和可恢復項目，不只顯示通用錯誤。
- 驗證註解移除後仍是合法 Gemote YML。

驗收條件：

- 編輯器輸出 YML → 反解析 → 再輸出，圖層與動畫語意一致。
- 被修改或缺少部分註解時仍可恢復有效粒子。
- 超過負載上限時不得靜默降低品質，必須顯示報告。

### Phase 8：編輯工作流與可靠性

工作內容：

- Command 模式 Undo/Redo，涵蓋畫點、刪除、Transform、關鍵影格、圖層拖放和匯入。
- 圖層複製、群組複製、多選、批次顯示與批次匯出切換。
- 自動儲存版本和損毀恢復。
- 拖放增加 before/after/inside 指示及循環父子關係防護測試。
- 所有新文字納入三語翻譯檔。

驗收條件：

- 至少可連續 Undo/Redo 50 個跨類型操作。
- 瀏覽器重整後能恢復最近一次自動儲存。

### Phase 9：測試、文件與正式驗收

工作內容：

- 單元測試：schema、插值、loop、修改器、YML、預算演算法。
- 整合測試：圖片 → 圖層 → 動畫 → 儲存 → YML → 反解析。
- 瀏覽器 E2E：素材庫、拖放、時間軸和下載流程。
- 壓力測試：1K、10K、50K 粒子與 100 張素材。
- 更新繁中、簡中、英文 README 和 FILE_MAP。
- 建立 A/B 雙旋轉與多環公轉範例專案。

正式完成條件：

- 所有測試通過，沒有未處理瀏覽器錯誤。
- 舊專案可載入，新專案可完整重載。
- A/B 反向 360°、群組動畫、YML round-trip 全部通過。
- 10K 粒子效能達到約定門檻，匯出負載有明確預警。
- 文件足以讓新使用者從圖片建立並匯出第一個複雜動畫。

## 5. 建議執行順序

建議依序執行：Phase 1 → 2 → 3 → 4 → 7 → 5 → 6 → 8 → 9。

理由是先固定動畫資料與匯出語意，再建立素材庫和效能優化；否則後續可能因 schema 或求值器變更而重做資產及介面整合。

## 6. 風險與控制方式

- **YML 指令量暴增**：匯出前估算、品質模式、硬上限和分層預算。
- **多圈旋轉插值錯誤**：角度保留原始圈數，不做 modulo 正規化。
- **父子 Transform 順序錯誤**：以固定矩陣順序及 fixture 測試。
- **圖片素材占用過大**：縮圖與原圖分開保存，顯示儲存空間並支援清理。
- **主執行緒凍結**：轉換與烘焙使用 Worker、分批回報進度。
- **YML 註解遭外部工具移除**：仍保留無註解粒子反解析 fallback。
- **舊專案不相容**：每次 schema 變更提供 migration fixture。

## 7. 預估里程碑

- M1：Phase 1–2，穩定 MVP 與完整 Transform。
- M2：Phase 3–4，完整關鍵影格與複雜旋轉動畫。
- M3：Phase 7，可靠烘焙、負載報告與 YML round-trip。
- M4：Phase 5–6，素材庫與大型場景效能。
- M5：Phase 8–9，Undo/Redo、整合測試、文件與正式驗收。

本計畫不以「介面已出現」作為完成判定；每個里程碑必須同時具備資料持久化、預覽、匯出、反解析及測試證據。
